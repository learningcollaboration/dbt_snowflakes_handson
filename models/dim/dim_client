WITH src_client AS (
 SELECT
 *
 FROM
 {{ ref('src_client') }}
)
SELECT
client_id
,sex
,to_char(to_date(fulldate,'YYYY-MM-DD'),'YYYYMMDD') AS date_dim_id
,age
,city
,state
,zipcode
,district_id
FROM
src_client